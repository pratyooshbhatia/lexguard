# Required — server-side only, never exposed to the browser.
GEMINI_API_KEY=

# Optional — override model. Must support generateContent + JSON output.
GEMINI_MODEL=gemini-2.5-flash

# Optional — soft cap on uploaded file size (bytes). Default: 10MB.
LEXGUARD_MAX_UPLOAD_BYTES=10485760
